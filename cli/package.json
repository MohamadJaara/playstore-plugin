{
  "name": "@playstore-plugin/cli",
  "version": "0.1.0",
  "description": "Read-only local CLI foundation for the Play Store Codex plugin.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "playstore": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "commander": "^15.0.0",
    "google-auth-library": "^10.9.1",
    "zod": "^4.6.2"
  },
  "devDependencies": {
    "@types/node": "^24.13.4",
    "typescript": "^6.0.3",
    "vitest": "^4.1.11"
  },
  "engines": {
    "node": ">=24.21.0"
  }
}
